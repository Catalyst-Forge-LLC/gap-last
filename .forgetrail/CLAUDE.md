# Claude / coding-agent notes (Gap Last)

Read `GENESIS.md` before building. ForgeTrail state lives in
`.forgetrail/workflow_tracking.json`. Do not copy a methodology tree
into this repo.

Commit messages: no unrequested attribution trailers. This repo's
`.cursor/rules/git-user-commits.mdc` is an explicit exception that
asks for `Co-authored-by: Cursor <cursoragent@cursor.com>`.

Never `npm publish`. Never create a GitHub remote unless asked.
